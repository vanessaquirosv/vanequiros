name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Firebase config (from secrets)
        env:
          FB_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FB_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FB_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FB_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FB_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FB_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: |
          if [ -z "$FB_API_KEY" ] || [ -z "$FB_APP_ID" ]; then
            echo "::error::Faltan secrets FIREBASE_* en Settings → Secrets → Actions"
            exit 1
          fi
          mkdir -p js/web-rifa
          node -e "
          const fs = require('fs');
          const cfg = {
            apiKey: process.env.FB_API_KEY,
            authDomain: process.env.FB_AUTH_DOMAIN,
            projectId: process.env.FB_PROJECT_ID,
            storageBucket: process.env.FB_STORAGE_BUCKET,
            messagingSenderId: process.env.FB_MESSAGING_SENDER_ID,
            appId: process.env.FB_APP_ID,
          };
          fs.writeFileSync(
            'js/web-rifa/firebase-config.js',
            'export const firebaseConfig = ' + JSON.stringify(cfg, null, 2) + ';\n'
          );
          console.log('firebase-config.js generado OK');
          "

      - name: Generate Tech & Chic Firebase config (from secrets)
        env:
          TC_FB_API_KEY: ${{ secrets.TC_FIREBASE_API_KEY }}
          TC_FB_AUTH_DOMAIN: ${{ secrets.TC_FIREBASE_AUTH_DOMAIN }}
          TC_FB_PROJECT_ID: ${{ secrets.TC_FIREBASE_PROJECT_ID }}
          TC_FB_STORAGE_BUCKET: ${{ secrets.TC_FIREBASE_STORAGE_BUCKET }}
          TC_FB_MESSAGING_SENDER_ID: ${{ secrets.TC_FIREBASE_MESSAGING_SENDER_ID }}
          TC_FB_APP_ID: ${{ secrets.TC_FIREBASE_APP_ID }}
        run: |
          if [ -z "$TC_FB_API_KEY" ] || [ -z "$TC_FB_PROJECT_ID" ]; then
            echo "::warning::Secrets TC_FIREBASE_* no configurados — Tech & Chic se publica en modo demo (catálogo local)."
            exit 0
          fi
          node -e "
          const fs = require('fs');
          const cfg = {
            apiKey: process.env.TC_FB_API_KEY,
            authDomain: process.env.TC_FB_AUTH_DOMAIN,
            projectId: process.env.TC_FB_PROJECT_ID,
            storageBucket: process.env.TC_FB_STORAGE_BUCKET,
            messagingSenderId: process.env.TC_FB_MESSAGING_SENDER_ID,
            appId: process.env.TC_FB_APP_ID,
          };
          fs.writeFileSync(
            'tech-and-chic/js/firebase-config.js',
            'export const firebaseConfig = ' + JSON.stringify(cfg, null, 2) + ';\n'
          );
          console.log('tech-and-chic/js/firebase-config.js generado OK');
          "

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
