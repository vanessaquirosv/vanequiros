/**
 * Plantilla de configuración Firebase — web-rifa
 * Copia este archivo como firebase-config.js y rellena los valores.
 * firebase-config.js está en .gitignore y NO debe subirse a GitHub.
 *
 * Obtén los valores en: Firebase Console → Project Settings → Your apps → Web app
 */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "vanessaquiros-co.firebaseapp.com",
  projectId: "vanessaquiros-co",
  storageBucket: "vanessaquiros-co.firebasestorage.app",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
